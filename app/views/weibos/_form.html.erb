<%= form_for(@weibo) do |f| %>
  <% if @weibo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weibo.errors.count, "error") %> prohibited this weibo from being saved:</h2>

      <ul>
      <% @weibo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.text_area :body,:rows => '4',:style=>'width:100%' %>
  </div>
  <div class="actions">
    <%= f.submit '发布微博',:class => 'btn btn-primary' %>
  </div>
<% end %>